<?xml version="1.0" encoding="UTF-8"?>
<!--
 * Scilab ( http://www.scilab.org/ ) - This file is part of Scilab
 * Copyright (C) DIGITEO - 2010 - Allan CORNET
 *
 -->
<refentry version="5.0-subset Scilab" xml:id="csv_read" xml:lang="en"
          xmlns="http://docbook.org/ns/docbook"
          xmlns:xlink="http://www.w3.org/1999/xlink"
          xmlns:svg="http://www.w3.org/2000/svg"
          xmlns:ns3="http://www.w3.org/1999/xhtml"
          xmlns:mml="http://www.w3.org/1998/Math/MathML"
          xmlns:db="http://docbook.org/ns/docbook">
  <info>
    <pubdate>$LastChangedDate$</pubdate>
  </info>

  <refnamediv>
    <refname>read_csv</refname>

    <refpurpose>Read comma-separated value file</refpurpose>
  </refnamediv>

  <refsynopsisdiv>
    <title>Calling Sequence</title>

    <synopsis>M = csv_read(filename)</synopsis>
    <synopsis>M = csv_read(filename, separator)</synopsis>
    <synopsis>M = csv_read(filename, separator, decimal)</synopsis>
    <synopsis>M = csv_read(filename, separator, decimal, conversion)</synopsis>
    <synopsis>M = csv_read(filename, separator, decimal, conversion, substitute)</synopsis>
    <synopsis>M = csv_read(filename, separator, decimal, conversion, substitute, range)</synopsis>    
  </refsynopsisdiv>

  <refsection>
    <title>Parameters</title>

    <variablelist>
      <varlistentry>
        <term>filename</term>

        <listitem>
          <para>a character string. The file path.</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>separator</term>

        <listitem>
          <para>a character string. Field separator used, default value is "," or [] uses current default</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>decimal</term>

        <listitem>
          <para>a character string. Field decimal used, default value is "." or [] uses current default</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>conversion</term>

        <listitem>
          <para>a character string. Field conversion used ("string" or "double"), default value is "string" or [] uses current default</para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term>substitute</term>

        <listitem>
          <para>a matrix of  string. ["searched" , "replaced"] to replace a field by another.</para>
        </listitem>
      </varlistentry>


      <varlistentry>
        <term>range</term>

        <listitem>
          <para>a character string. reads only the range specified.
Specify range using the notation [R1 C1 R2 C2] where (R1,C1) is the upper left corner of the data to be read and (R2,C2) is the lower right corner.</para>
<para>[]: all range </para>
        </listitem>
      </varlistentry>

      

      <varlistentry>
        <term>M</term>

        <listitem>
          <para>a matrix of strings or double.</para>
        </listitem>
      </varlistentry>
    </variablelist>
  </refsection>

  <refsection>
    <title>Description</title>

    <para>Given an ascii file with delimited fields, for instance created by a
    spreadsheet using "Text and comma" format, csv_read(filename) returns the
    corresponding Scilab matrix of strings. Use csv_read(filename, separator) for
    another choice of separator.</para>
    <para>with parameter "conversion" equals to "double" strings are converted as NaN.</para>

  </refsection>

  <refsection>
    <title>Examples</title>

    <programlisting role="example">// create a file with some data separated with tab    
A = 1:50;
csv_write(A, TMPDIR + '/foo.csv', ascii(9), '.')
mgetl(TMPDIR + '/foo.csv')

// read csv file
B = csv_read(TMPDIR + '/foo.csv',ascii(9))

// as double
C = csv_read(TMPDIR + '/foo.csv',ascii(9), '.', 'double')

// compares original data and result
and(A == C)

M = [%inf,%nan];
filename = fullfile(TMPDIR,'datas.csv');
csv_write(M, filename);
mgetl(filename)
N = csv_read(filename,",",".","double",["Nan", "0"])

</programlisting>
  </refsection>

  <refsection>
    <title>See Also</title>

    <simplelist type="inline">
      <member><link linkend="csv_write">csv_write</link></member>
    </simplelist>
  </refsection>

  <refsection>
    <title>Authors</title>

    <variablelist>
      <varlistentry>
        <term>Allan CORNET</term>
      </varlistentry>
    </variablelist>
  </refsection>
</refentry>
